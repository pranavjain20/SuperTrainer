[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    smoke: real Anthropic API calls; costs money; excluded from default run. Use: pytest -m smoke
addopts = -m "not smoke"
