# Days 11-12 — Feb 23, 2026

*Phase 1b begins. Planning, building, researching — and a day of real learning about how to work with AI.*

---

## What happened today

Two sessions in one day. The morning was planning the voice pipeline architecture. The afternoon split into two parallel tracks: Claude Code built the transcription service and exercise database structure while I deep-researched individual exercises in a separate Claude cloud chat, trying to create the best possible exercise dictionary.

The building got done. But the real story of today is what I learned — about AI, about ownership, about how to actually use these tools effectively. I didn't ship a perfect exercise file today. I shipped a methodology and a mindset shift.

### The planning (morning)

Mapped the voice pipeline into four components and seven days. Learned about tool_use — how it turns Claude from a chatbot into a structured extraction system. Learned about Deepgram's keyterm prompting — a 100-term cheat sheet so the speech-to-text engine doesn't mangle gym vocabulary. Broke Phase 1b into collaborative days (AI decisions) and independent days (infrastructure). This is where the first spark of curiosity about the AI side happened.

### The building (afternoon — Claude Code side)

Created `exercise_db.json` with 97 exercises — canonical names, aliases, categories, primary and secondary muscles, equipment, difficulty, and detailed common errors with coaching context. Built `services/transcription.py`: `load_exercise_db()` reads the JSON, `build_keyterm_list()` extracts the top 100 keyterms for Deepgram (priority order: gym jargon first, then abbreviations like RDL/OHP, then multi-word exercise names — common single words like "squat" are skipped because Deepgram handles those fine), and `transcribe_audio()` sends audio bytes to Deepgram Nova-3 and returns a structured result with transcript, confidence, duration, and word-level timing data. Updated `seed.py` to load exercises from the JSON instead of 160 lines of hardcoded dicts — uses SQLAlchemy introspection so if the Exercise model gains a new column, seed picks it up automatically without a code change. 24 new tests, 330 total, all green.

### The research (afternoon — my parallel track)

This is where most of my time and energy went. I opened Claude cloud chat and started researching exercises to make the database genuinely expert-level. Common errors with coaching context, anatomical muscle names, detailed aliases. The idea was to create something a physiotherapist would approve of, not just a list of exercise names.

It didn't go as planned — and that's where the learning happened.

## Understanding the architecture

Before building, I needed to understand something that confused me: if Deepgram only accepts 100 keyterms, why are we building a JSON file with 97 exercises and hundreds of fields? Aren't they the same thing?

They're not. The JSON file is the full exercise dictionary — unlimited, as detailed as we want. It's the single source of truth used by multiple systems: the Deepgram keyterm list is derived from it (top 100 terms extracted at runtime), the parser will fuzzy-match against it (Day 4), and The Brain will use it for domain knowledge (Phase 3). The 100-term limit is just one downstream consumer. The database itself has no size constraint.

This was a small but important moment — understanding how one file feeds into multiple systems at different phases. It's the kind of architectural thinking I want to get better at.

## Reflections

Today's reflections are the core of this devlog. I didn't ship a finished exercise file, but I learned things about working with AI that I'll carry through the rest of this project and beyond.

### It feels like my product now

Today felt completely different from Phase 1a. I spent real hours doing real research — not reviewing code, not approving plans, actually doing the work myself. And that flipped something fundamental. It stopped feeling like "Claude is building it for me" and started feeling like my product. My architecture, my brain. Claude is the engineer whose job is to execute, but I'm the one with the vision, the domain knowledge, the decisions. We're building it together, but the ownership is mine.

That distinction matters. During Phase 1a, things felt mechanical — "Claude plans, I check in, Claude builds, I have no idea how the pieces fit together." Today I was in the work. I was the one deciding what a good exercise entry looks like, what level of detail matters, what a trainer would actually need. And because I was doing the thinking, the product started feeling like mine.

### You can't just say "make it the best"

This was the biggest, most painful lesson. My first instinct when starting the exercise research was to open Claude cloud and say something like: "I want to create the best possible exercise database. Do very deep research — journals, articles, textbooks — and make this as good as the world's best JSON document about exercises."

I didn't specify what kind of research. I didn't specify what to look into. I didn't specify how Claude should approach it or what output format I wanted. I just said "make it the best" and thought that would be enough for a smart AI model.

It wasn't.

The context window expired. I had no idea what thinking Claude had done. I couldn't evaluate the output because I wasn't part of the process. I couldn't add my own judgment or intuition because I'd delegated the entire thing wholesale. And now I had a used-up context window and nothing to show for it.

This is the trap: because Claude has access to the internet and is incredibly knowledgeable, it's tempting to think that delegating everything to it will produce better results than doing it yourself. But that's wrong. This is not my statistics homework where AI gets a better grade than me. This is a real product with no rubric and no right answer. Just because Claude can scan the internet faster than me doesn't mean I should hand over the thinking.

### The methodology that actually worked

After wasting two hours on the "just make it great" approach, I figured out the right workflow — and it was completely different:

1. Ask Claude to do deep research on *one* exercise (Nordic Hamstring Curl)
2. Have Claude explain how it would research, what it would look into, how it would structure the output
3. Review the result together — back and forth on what to keep, what to remove, what the right level of detail is
4. Agree on a template: canonical name, aliases, category, primary muscles (anatomical names), secondary muscles, equipment, difficulty, common errors with coaching context. No research_notes (that belongs in RAG, not this JSON).
5. *Then* — and only then — parallelize. Give 15 different Claude instances 10 exercises each, all following the agreed methodology.

That's when things clicked. The difference: in the first approach, I delegated both the thinking and the execution. In the second approach, I owned the thinking (methodology, format, quality bar) and delegated only the execution. The AI works for me, not the other way around.

But it took the entire day to learn this. Two hours figuring out logistics, frying tokens on approaches that didn't work, getting context windows to expire before producing useful output. From a pure output perspective, I couldn't even finish the exercise file today. But from a learning perspective, I landed on a methodology I'll use for the rest of this project.

### Voice changes the dynamic with AI

I'm talking to Claude Code through voice transcription, and I noticed something important: I'm saying 50x more than I would type. When you type, you naturally edit yourself — you compress, you cut fluff, you front-load the important stuff. When you talk, you ramble. You tell stories. You bury the key point in the middle of a paragraph.

This matters because of how these models work. If I give Claude a very long prompt and the important information is in the middle, it gets less attention. This is the "lost in the middle" problem — models pay most attention to the beginning and end of long inputs. When I'm talking for minutes at a time, the critical instruction might be sentence 47 out of 60.

And it compounds: every long message I send to Claude Code takes up context tokens. That's space that could be used for code, for file reads, for actual work. If Claude doesn't get the important information because it's buried in a wall of voice-transcribed text, it might build something I don't want, or forget something I told it that I think is important. That might even explain why Claude sometimes seems to "forget" things I've told it — not because it can't remember, but because the information was buried in a long conversational message and didn't get the weight it deserved.

I don't have a perfect solution yet. But being aware of this changes how I should communicate: front-load the important stuff, keep prompts focused, be more intentional about what I say versus what I think out loud.

### Context windows aren't unlimited

Even with Claude Pro Max, context windows end. I learned this the hard way — mid-research, the cloud chat hit its limit and I lost all the accumulated context. I hadn't thought about context as a finite resource before. Now I know: be smart about it. Plan for it. Save intermediate outputs. Don't assume you can have one infinitely long conversation.

### Agency — the real lesson

Everyone talks about agency being the new moat. What separates someone from others. What makes someone the best. I've heard this abstractly a hundred times. Today I experienced what it actually means.

Agency comes from working on something tirelessly and iterating. Not from delegating to AI and hoping for the best. Not from treating this like schoolwork where you hand in the assignment and collect the grade. Agency comes from using your brain, using your intuition, making it your own.

I can't keep thinking of this like schoolwork. There is no rubric. There is no right or wrong answer. There is only me — my vision for what this product should be, my understanding of what trainers need, my judgment about what matters and what doesn't. Claude can execute, but it can't want something. It can't care about the product the way I do. It can't make the judgment calls that come from actually understanding the domain.

Claude Code is great because it feels like playing a video game — I want to keep iterating, keep beating levels. I'm excited by this space, I have intuition, I have passion, I want to work on this all day. That energy is there. But it has to be channeled the right way. I have to bring presence, sharpness, actual thinking — not just delegation.

You don't build agency by delegating to AI. You build agency by using your brain, using your intuition, making it your own, and then using AI as a tool that can do things — but as your engineer, under your direction. It's not the AI's product. It's your product. There's a difference between the two, and I learned that difference today.

### The first spark (morning)

Before all the afternoon chaos, the morning planning session had its own important moment. I finally understood tool_use — how it turns Claude from a chatbot into a structured extraction system. You define a schema matching your database model, and the LLM fills it in like a form. The schema isn't generic; it's `SessionEntry`, built over ten days of Phase 1a work. That's domain knowledge encoded into the interface between AI and the system.

This answered the "ChatGPT wrapper" question I'd been carrying for weeks. Without tool_use, you'd call Claude and get text, then need a second parser to structure it. With tool_use, Claude *is* the parser. The intelligence isn't in the model — it's in the tool schema, the system prompt, the validation rules. It's the fact that I know a trainer says "5 sets of squats at 80 kilos, RPE 8, knees caving on the last two" and I know that means one exercise card with 5 set records, a weight field, an RPE field, and a form note. Claude doesn't know that by default. It knows because I told it — through a schema I designed based on a data model I built based on understanding how trainers actually work.

And this connects to bottom-up learning. If I'd seen tool_use in a YouTube video, I would've thought "cool, function calling" and moved on. But because I need it for something real — to parse trainer voice notes into my specific database structure — the concept is concrete. The questions come from the problem, the learning follows the questions. "What is tool_use?" becomes "how do I make Claude output structured data that matches a specific Postgres table I built?"

That curiosity was missing during the Phase 1a CRUD grind. It's here now.

---

## Late Night Update

I was supposed to come back tomorrow and run the batches. Instead I came back the same night and finished the entire exercise database. Couldn't let it go.

### What I actually built

Not just a JSON file. A research system.

**The v3 rejection.** First thing I did was ask Claude to expand the 101-exercise v2 into 250-350 exercises using deep research. It produced 212. Looked complete — zero duplicates, schema-perfect. I asked one question: did you actually run the searches? The honest answer was no. The 212 exercises were written from training knowledge, not from research. "Hamstrings" instead of "biceps femoris long head." Generic one-liner errors instead of coaching context. I rejected all 212 and started over.

**One exercise at a time.** Instead of fixing 212 exercises in bulk, I picked one — Nordic Hamstring Curl — and demanded the full research process for that single entry. Three actual web searches. The first found 27 aliases across academic literature (NHE, NHC), coaching communities, bodybuilding forums, physical therapy documentation, and Eastern European sports science. The second found a 2025 systematic review (619 participants) showing Nordics preferentially activate semitendinosus over biceps femoris — the opposite of prone leg curls. The third produced 5 coaching-grade errors with biomechanical WHY context, not just descriptions. That one entry became the gold standard.

**The schema lock.** After confirming the Nordic entry, I locked the schema: canonical_name, aliases, category, primary_muscles (specific anatomy), secondary_muscles (new — EMG research naturally produces it), equipment, difficulty, common_errors (detailed coaching descriptions). Removed research_notes — useful data but belongs in RAG, not a workout logging JSON. Nothing extra, nothing missing.

**The prompt engineering.** I needed 150 exercises at Nordic quality across 17 separate Claude windows. Built a reusable research prompt, then realized it was too long — Claude would skim it, skip the mandatory searches, and produce the same surface-level output I'd rejected. Restructured around one principle: front-load the constraint. The first thing the prompt says is that all three web searches are a mandatory gate before writing any JSON. Exact search string templates. No ambiguity about what "deep research" means. Cut the prompt roughly in half, made it significantly more directive.

**The two-file split.** PROMPT.md (the research protocol, never changes, two placeholders) and BATCHES.md (150 exercises in 17 batches of 9, plus a running completed list). Each Claude window only sees its 9 exercises, not the other 141. Clean, no wasted tokens.

**The 150 exercise list.** Curated by one criterion: how often would a real athlete or coach say this exercise out loud during a training session? Covers all 7 primary movement patterns. Every major muscle group has a primary exercise and 2-3 variants. Rehab/prehab covers the most injury-prone areas in athletic populations. Olympic lifts included because every elite S&C program uses them. Strongman movements included because sled pushes and carries are now standard in professional athletic prep. Stretches and mobility drills explicitly excluded — coaches don't log these in workout apps.

**Model choice.** Sonnet 4.6 — strong enough for both research and structured output, fast, cost-effective. Opus would be overkill; the task is search-and-synthesize, not deep reasoning. Extended thinking wrong tool — the bottleneck is information retrieval, not reasoning depth. Used claude.ai interface because web search behavior is more natural and reliable there than via API.

**Parallelization.** 17 batches are completely independent. Opened multiple Claude windows simultaneously. All 150 exercises in one session instead of 17 sequential sessions.

### The result

Ran the batches. Merged the outputs. Brought back 142 exercises (8 from the original 150 got consolidated as variants of other exercises during research). Swapped into `exercise_db.json`. Claude Code wrote 36 tests proving the improvement:

| Metric          | Old DB | New DB  | Change      |
|-----------------|--------|---------|-------------|
| Exercises       | 97     | 142     | +46%        |
| Total aliases   | 321    | 3,466   | **10.8x**   |
| Avg aliases/ex  | 3.3    | 24.4    | **7.4x**    |
| Min aliases     | 1      | 16      | 16x floor   |
| Unique muscles  | 55     | 115     | 2.1x        |
| Generic muscles | 6      | 0       | Gone        |
| Common errors   | 210    | 706     | 3.4x        |

366 total tests passing. Day 1 of Phase 1b is done.

### What this system actually is

Not a JSON file. It's a research methodology with mandatory quality gates, a gold standard benchmark (the Nordic entry) that all future work gets measured against, a reusable prompt engineered to force the right research behavior across unlimited context windows, and a scalable system that works the same way at exercise 10 or exercise 200.

The quality comes from one decision made early: rejecting the v3 expansion that looked complete but wasn't backed by real research. Everything else — the one-exercise methodology, the schema lock, the prompt restructuring, the two-file split, the parallelization — flows from that single decision to not ship something that wasn't right.

## What's next

Day 2 — the Claude parser. Tool_use schema, system prompt, `services/parser.py`. The first real AI component we build together, collaboratively. This is where tool_use stops being a concept and becomes actual code that turns trainer speech into structured data.
